<div class="container-fluid">

    <div class="card-body">
        <div class="table-responsive">
            <div class="header_table">
                <p>Data Feedback
                </p>
            </div class="table_field">
            <table class="table table-bordered" id="dataTable" width="100%" cellspacing="0">
                <thead class="data-table">
                    <tr>
                        <th>No</th>
                        <th>Pemesan</th>
                        <th>Review</th>
                        <th>Service</th>
                        <th>Nama Tukang</th>
                        <th>Delete</th>
                    </tr>
                </thead>
                <tbody>
                    <% let no=1; data.forEach(element=> { %>
                        <tr>
                            <th scope="row">
                                <%= no %>
                            </th>
                            <td>
                                <%= element.pemesan.name %>
                            </td>
                            <td>
                                <%= element.review %>
                            </td>
                            <td>
                                <%= element.tukang.service %>
                            </td>
                            <td>
                                <%= element.tukang.name %>
                            </td>
                            <td>
                                <a type="button" href="/feedback/delete/<%= element.id %>"
                                    class="btn btn-danger">Delete</a>
                            </td>
                        </tr>

                        <% no++ }) %>
                </tbody>
            </table>
        </div>
    </div>
</div>